services:
  - type: web
    name: atelier-habitat
    env: python
    buildCommand: ./build.sh
    startCommand: gunicorn config.wsgi
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: atelier-habitat.onrender.com
