function onScroll(){const h=document.documentElement;const st=h.scrollTop||document.body.scrollTop;const sh=h.scrollHeight-h.clientHeight;document.getElementById('progressBar').style.width=(st/sh)*100+'%';document.getElementById('backToTop').style.display=st>300?'block':'none';document.querySelectorAll('.reveal').forEach(el=>{if(el.getBoundingClientRect().top<window.innerHeight-80)el.classList.add('visible')})}
window.addEventListener('scroll',onScroll);window.addEventListener('load',onScroll);document.getElementById('backToTop')?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
function toggleTheme(){const html=document.documentElement;const n=html.dataset.theme==='dark'?'light':'dark';html.dataset.theme=n;localStorage.setItem('theme',n)}
window.addEventListener('load',()=>{document.documentElement.dataset.theme=localStorage.getItem('theme')||'light'})
function initAjaxComment(url){const form=document.getElementById('commentForm');if(!form)return;form.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);const res=await fetch(url,{method:'POST',headers:{'X-Requested-With':'XMLHttpRequest'},body:fd});const data=await res.json();if(data.ok){document.getElementById('commentList').insertAdjacentHTML('afterbegin',`<div class="comment-box"><strong>${data.name}</strong><small>${data.created_at}</small><p>${data.message}</p></div>`);form.reset();}})}
